{$IFDEF UNIX}
{$IFNDEF NO_NCURSES}
{$DEFINE USE_NCRT}
{$ENDIF}
{$ENDIF}

{$IF not DEFINED(MSWINDOWS) and (DEFINED(FPC) and not DEFINED(USE_NCRT))}
{$DEFINE HAS_SCREENHEIGHTWIDTH}
{$ENDIF}

{$IF DEFINED(FPC) and DEFINED(UNIX) and DEFINED(USE_X)}
{$DEFINE FPC_UNIX}
{$ENDIF}

{
vi:ft=pascal
}
